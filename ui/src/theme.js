import { createTheme } from "@material-ui/core/styles";

const theme = createTheme({
  palette: {
    primary: {
      main: "#6200EE",
    },
    // secondary: {
    //   main: "#fff",
    // },
    // background: {
    //   default: "#fff",
    // },
  },
});

export default theme;
